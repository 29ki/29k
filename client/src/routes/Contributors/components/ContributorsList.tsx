import styled from 'styled-components/native';
import Gutters from '../../../common/components/Gutters/Gutters';

export const ContributorsList = styled(Gutters)({
  flexDirection: 'row',
  flexWrap: 'wrap',
});
