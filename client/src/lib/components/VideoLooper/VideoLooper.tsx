import {requireNativeComponent} from 'react-native';
import {VideoLooperProperties} from '../../../../types/VideoLooper';

export default requireNativeComponent<VideoLooperProperties>('VideoLooper');
