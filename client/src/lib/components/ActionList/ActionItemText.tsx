import styled from 'styled-components/native';
import {Body16} from '../Typography/Body/Body';

const ActionItemText = styled(Body16).attrs({
  numberOfLines: 1,
})({
  flex: 1,
});

export default ActionItemText;
